// ponytail: tạm mở nhắn tin cho mọi cặp client/worker. Set true để bật lại luật
// "chỉ được chat khi đã có booking confirmed" (HTTP + socket + lọc danh sách hội thoại).
export const DIRECT_CHAT_BOOKING_GATE_ENABLED: boolean = false;
