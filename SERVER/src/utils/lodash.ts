import _ from "lodash";

export default _;

export const {
  cloneDeep,
  debounce,
  throttle,
  isEmpty,
  isEqual,
  merge,
  omit,
  pick,
  uniq,
  uniqBy,
  groupBy,
  keyBy,
  orderBy,
  chunk,
  flatten,
  flattenDeep,
} = _;
