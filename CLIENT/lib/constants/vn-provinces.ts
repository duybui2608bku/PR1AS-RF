/** Public Province Open API v2 base (server-side proxy uses same host). */
export const VN_PROVINCES_OPEN_API_V2 = "https://provinces.open-api.vn/api/v2";

/** Max work areas per worker profile (must match server Zod). */
export const WORK_LOCATIONS_MAX = 20;
